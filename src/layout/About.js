import React from 'react';
import { SCards, SectionWrap, SubTitle, Title } from '../components/StyledComponents';

const About = () => {
	return (
		<SectionWrap>
			<Title>About Page</Title>
			<SCards>
				<SubTitle>Card Active</SubTitle>
			</SCards>
		</SectionWrap>
	);
};

export default About;
